apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: dp-task1
spec:
  steps:
    - name: echo
      image: alpine
      script: |
        #!/bin/sh
        $(workspaces.source-1.path)/pipelines/scripts/dp-task1-script.sh